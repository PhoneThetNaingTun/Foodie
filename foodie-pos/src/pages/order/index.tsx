import OrderLayout from "@/components/OrderLayout";
import { Box } from "@mui/material";

const orderApp = () => {
  return (
    <OrderLayout>
      <Box>Order app</Box>
    </OrderLayout>
  );
};

export default orderApp;
